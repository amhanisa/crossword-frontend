import Home from "../view/pages/Home";
import Score from "../view/pages/Score";
import Winner from "../view/pages/Winner";

const routes = {
  "/": Home,
  "/home": Home,
  "/score": Score,
  "/winner": Winner,
};

export default routes;
